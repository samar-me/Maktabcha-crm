"use client";import*as React from"react";import{Gift,Loader2}from"lucide-react";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{getReferralDashboardAction}from"@/actions/referrals";export function ReferralDashboardCard(){const[d,setD]=React.useState<any>(null);React.useEffect(()=>{getReferralDashboardAction().then(r=>r.success&&setD(r.data))},[]);return<Card className="border-violet-200 dark:border-violet-900"><CardHeader className="pb-3"><CardTitle className="text-base flex items-center gap-2"><Gift className="w-5 h-5 text-violet-600"/>Referral Program — bu oy</CardTitle></CardHeader><CardContent>{!d?<Loader2 className="animate-spin"/>:<div className="grid grid-cols-2 sm:grid-cols-5 gap-3">{[["Referral",d.total],["Qualified",d.qualified],["Pending",d.pending],["Referral revenue",`${Number(d.revenue).toLocaleString()} so‘m`],["Berilgan discount",`${Number(d.discount).toLocaleString()} so‘m`]].map(([k,v])=><div key={String(k)} className="rounded-xl bg-muted/50 p-3"><p className="text-[11px] text-muted-foreground">{k}</p><p className="font-bold mt-1">{v}</p></div>)}</div>}</CardContent></Card>}
